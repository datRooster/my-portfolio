/** @type {import('next').NextConfig} */
const nextConfig = {
  // ✅ CONFIGURAZIONE EXPORT STATICO PER HOSTING CONDIVISO ARUBA
  output: 'export',
  trailingSlash: true,
  
  // ✅ Ottimizzazioni per hosting statico
  images: {
    unoptimized: true, // Disabilita ottimizzazione immagini (non disponibile in statico)
  },
  
  // ✅ Disabilita funzionalità server-side non supportate
  distDir: 'out', // Directory output per FTP upload
  
  // ✅ Headers per file statici (gestiti da .htaccess)
  async headers() {
    return []; // Headers gestiti da Apache
  },
  
  // ✅ Esclude API routes dalla build statica
  async rewrites() {
    return [];
  },
  
  // ✅ Configurazione per build statico
  experimental: {
    esmExternals: false,
  },
  
  // ✅ TypeScript e ESLint per build pulito
  typescript: {
    ignoreBuildErrors: false,
  },
  eslint: {
    ignoreDuringBuilds: true,
  }
};

module.exports = nextConfig;